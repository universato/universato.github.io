# 正規表現

Regular Expression

## よく使う

* `\n` 改行
* `$` 行末

ファイルの末尾は、行末だとしても、改行だとは限らない。

* `\d` 数字1桁
* `\d+` 数字1桁以上
* `\d{1,2}` 数字(1桁〜2桁)

* `you ?tube` -> YouTube
* `(\d{1,4})[-/](\d{1,2})[-/](\d{1,2})` -> $1年$2月$3日
